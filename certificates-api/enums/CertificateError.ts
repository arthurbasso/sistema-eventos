export enum CertificateError {
    NotFound = 'Certificate not found',
    NotCreated = 'Certificate not created',
    NotUpdated = 'Certificate not updated',
    NotDeleted = 'Certificate not deleted'
}